name: skills-sync

on:
  repository_dispatch:
    types: [skills-updated]
  schedule:
    - cron: "41 3 * * *"
  workflow_dispatch:
    inputs:
      ref:
        description: Optional immutable video-factory-skills release tag
        required: false
        type: string
      sha:
        description: Required peeled commit SHA when ref is provided
        required: false
        type: string
      package:
        description: Locked package to upgrade; defaults to video-factory-skills
        required: false
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      # Write credential: this repository's own token, used to push the sync branch
      # and open the pull request. It is never combined with the read credentials
      # below, so a token issued for reading an upstream library cannot authorize a
      # write here.
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      # Read credentials: one per upstream owner, selected by skill_vendor.py from
      # sources[].repo. Public sources still work without them.
      FULL_AIGC_SKILLS_SYNC_TOKEN: ${{ secrets.FULL_AIGC_SKILLS_SYNC_TOKEN }}
      FULL_STACK_SKILLS_SYNC_TOKEN: ${{ secrets.FULL_STACK_SKILLS_SYNC_TOKEN }}
      REQUESTED_REF: ${{ github.event.client_payload.ref || inputs.ref }}
      REQUESTED_SHA: ${{ github.event.client_payload.sha || inputs.sha }}
      REQUESTED_PACKAGE: ${{ github.event.client_payload.package || inputs.package || 'video-factory-skills' }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.12"
      - name: Vendor skills from pinned sources
        shell: bash
        run: |
          arguments=(update)
          if [[ -n "$REQUESTED_REF" || -n "$REQUESTED_SHA" ]]; then
            if [[ -z "$REQUESTED_REF" || -z "$REQUESTED_SHA" ]]; then
              echo "::error::Both ref and sha are required for a release upgrade"
              exit 1
            fi
            arguments+=(--source-ref "$REQUESTED_PACKAGE=$REQUESTED_REF")
            arguments+=(--expected-sha "$REQUESTED_PACKAGE=$REQUESTED_SHA")
          fi
          python3 scripts/vendor/skill_vendor.py "${arguments[@]}"
      - name: Verify the fresh snapshot
        run: python3 scripts/vendor/skill_vendor.py check
      - name: Push a sync branch when the snapshot changed
        id: push
        run: |
          if git diff --quiet && git diff --cached --quiet; then
            echo "vendored skills already current; nothing to sync"
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin refs/heads/chore/skills-sync:refs/remotes/origin/chore/skills-sync || true
          git checkout -B chore/skills-sync
          git add skills/ skills.lock.json
          git commit -m "chore: sync vendored skills from ${REQUESTED_PACKAGE} ${REQUESTED_REF:-current-lock}"
          git push --force-with-lease origin chore/skills-sync
          echo "changed=true" >> "$GITHUB_OUTPUT"
      - name: Open the sync PR if none exists
        if: steps.push.outputs.changed == 'true'
        run: |
          if gh pr view chore/skills-sync >/dev/null 2>&1; then
            echo "sync PR already open; branch updated"
            exit 0
          fi
          gh pr create \
            --title "chore: sync vendored skills from ${REQUESTED_PACKAGE} ${REQUESTED_REF:-current-lock}" \
            --body "Automated refresh of the checksummed skill snapshot from skills.lock.json. Source package: ${REQUESTED_PACKAGE}; requested release: ${REQUESTED_REF:-current lock}; expected commit: ${REQUESTED_SHA:-already pinned}. Externally managed skills come from the source recorded in skills.lock.json; plugin-local exceptions must be declared in plugin-local-skills.json." \
            --head chore/skills-sync --base main
